# Конфиг для второго Railway-сервиса (Telegram bot)
# Скопируйте в railway.toml или задайте Start Command в UI: npm run bot

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run bot"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
