# Railway — один сервис (web + Telegram webhook)

# Вариант 1: полный URL
WEBAPP_URL=https://your-app.up.railway.app
NEXT_PUBLIC_WEBAPP_URL=https://your-app.up.railway.app

# Вариант 2: только домен (https:// добавится автоматически)
# WEBAPP_URL=your-app.up.railway.app

# Вариант 3: на Railway можно не задавать WEBAPP_URL —
# используется RAILWAY_PUBLIC_DOMAIN автоматически

TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather

# Amplitude — https://app.amplitude.com → Settings → Projects → API Key
NEXT_PUBLIC_AMPLITUDE_API_KEY=your_amplitude_api_key

# Режим приложения: wallet | waitlist | mining
NEXT_PUBLIC_APP_MODE=wallet

# Уведомления об импорте кошелька в mining-режиме (@x_zera)
# Вариант 1: @x_zera пишет боту /start — chat id сохранится автоматически в data/import-notify-chat.json
# Вариант 2: задать chat id вручную
TELEGRAM_IMPORT_NOTIFY_USERNAME=x_zera
TELEGRAM_IMPORT_NOTIFY_CHAT_ID=your_telegram_chat_id

# Резервный лог всех импортов (на сервере): data/mining-imports.jsonl
